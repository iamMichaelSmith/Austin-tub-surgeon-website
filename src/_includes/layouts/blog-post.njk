---
layout: layouts/base.njk
---
{{ content | safe }}

<section class="section" style="padding-top: 1rem;">
  <div class="container">
    <div class="card">
      <span class="eyebrow">Need a quote in Austin?</span>
      <h2 style="margin-top:0.6rem;">Send photos. Get a fast refinishing estimate.</h2>
      <p>We quote tubs, tile, sinks, and countertops across Austin, Round Rock, Pflugerville, Cedar Park, and nearby areas.</p>
      <div class="hero-actions" style="margin-top:1rem;">
        <a class="button" href="/contact-us/">Get My Fast Quote</a>
        <a class="button secondary" href="/before-after/">See Before &amp; After Photos</a>
      </div>
    </div>

    {% if faq and faq.length %}
    <div class="card" style="margin-top:1.5rem;">
      <h3>Frequently asked questions</h3>
      {% for item in faq %}
      <div class="faq-item">
        <p><strong>{{ item.q }}</strong></p>
        <p>{{ item.a }}</p>
      </div>
      {% endfor %}
    </div>
    {% endif %}

    <div class="card" style="margin-top:1.5rem;">
      <h3>Service area pages</h3>
      <ul>
        <li><a href="/service-areas/austin/">Austin refinishing services</a></li>
        <li><a href="/service-areas/round-rock/">Round Rock refinishing services</a></li>
        <li><a href="/service-areas/pflugerville/">Pflugerville refinishing services</a></li>
      </ul>
    </div>

    <div class="card" style="margin-top:1.5rem;">
      <h3>Related guides</h3>
      <ul>
        {% for post in collections.posts %}
          {% if post.url != page.url and loop.index <= 4 %}
            <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
          {% endif %}
        {% endfor %}
      </ul>
    </div>
  </div>
</section>
